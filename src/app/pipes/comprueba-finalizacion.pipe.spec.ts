import { CompruebaFinalizacionPipe } from './comprueba-finalizacion.pipe';

describe('CompruebaFinalizacionPipe', () => {
  it('create an instance', () => {
    const pipe = new CompruebaFinalizacionPipe();
    expect(pipe).toBeTruthy();
  });
});
